import { reducer } from 'redux-form';

/**
 * The reducer of the forms module
 *
 * @kind function
 * @param {FormsState} state The state of redux-form
 * @param {Action} action A redux action
 * @returns {FormsState}
 */
export const formsReducer = reducer;
